{
  "name": "family-household-tracker",
  "version": "1.0.0",
  "description": "Family Household Tracker — Native Android App",
  "private": true,
  "scripts": {
    "sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
